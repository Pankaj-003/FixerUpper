# Components

The prototype uses repeated semantic Bootstrap navigation and footer markup in each static page so every page remains deployable as a standalone Vercel asset. Shared behavior is centralized in `frontend/js/common.js`, `auth.js`, and `cart.js`.
